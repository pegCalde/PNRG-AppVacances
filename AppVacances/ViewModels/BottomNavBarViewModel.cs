using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Xamarin.Forms;

namespace AppVacances.ViewModels
{
    public class BottomNavBarViewModel : TabbedPage
    {
		public BottomNavBarViewModel()
		{
			/*var navigationPage = new NavigationPage(new PageDetails());
			navigationPage.Title = "Details";

			Children.Add(new PageAccueil());
			Children.Add(navigationPage);
			Children.Add(new PageFavoris());*/
		}
	}
}